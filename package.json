{ 
  "name": "express_okki",
  "version": "1.0.0",
  "main": "server.js",
  "repository": "https://github.com/FibreOptics/express_okki.git",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd express_okki && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
