{ 
  "name": "express_okki",
  "version": "1.0.0",
  "main": "server.js",
  "repository": "https://github.com/FibreOptics/express_okki.git",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "yarn install --only=dev && yarn install && yarn run build"
  },
  "dependencies": {
    "express": "^4.16.4"
  }
}
